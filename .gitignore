# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
*~

# Local env overrides
.env
.env.*
docker-compose.override.yml

# PHPUnit / local caches
.phpunit.result.cache

# Runtime outputs (only affects untracked files)
tmp/*
coverage/*

# Composer/vendor local noise (tracked files in vendor will still appear if modified)
vendor/
vendor/composer/tmp-*
